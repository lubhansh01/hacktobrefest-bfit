# Supabase (public — safe to ship in the browser bundle).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Backend origin. Leave EMPTY in dev — vite.config.ts proxies /api to it.
# In production set it to the deployed API URL, e.g. https://api.example.com
VITE_API_URL=
